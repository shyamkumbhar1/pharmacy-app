name: Deploy to AWS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Safe Deploy to AWS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ubuntu
          key: ${{ secrets.AWS_SSH_KEY }}
          timeout: 30s
          command_timeout: 15m
          script: |
            set -e
            echo "🚀 Starting deployment..."
            echo "⏰ $(date)"
            
            echo ""
            echo "📥 Step 1/4: Pulling latest code..."
            cd ~/pharmacy-app
            git pull || { echo "❌ Git pull failed"; exit 1; }
            echo "✅ Code pulled successfully"
            
            echo ""
            echo "📦 Step 2/4: Checking Docker status..."
            cd Docker
            docker-compose ps
            
            echo ""
            echo "🏗️ Step 3/4: Building backend only (fast, ~2 minutes)..."
            echo "ℹ️ Frontend will be deployed manually (time-consuming)"
            echo "💡 Manual frontend deploy: cd ~/pharmacy-app/Docker && docker-compose build frontend && docker-compose up -d frontend"
            echo "⏰ Backend build start: $(date)"
            if ! timeout 300 docker-compose build backend-app backend-nginx backend-db --progress=plain; then
              echo "❌ Backend build failed or timed out - keeping old containers running"
              exit 1
            fi
            echo "⏰ Backend build complete: $(date)"
            echo "✅ Backend build successful"
            
            echo ""
            echo "🚀 Step 4/4: Restarting containers..."
            docker-compose up -d || { echo "❌ Failed to start containers"; exit 1; }
            echo "✅ Containers restarted"
            
            echo ""
            echo "⏳ Waiting for services to start..."
            sleep 5
            echo "✅ Wait complete"
            
            echo ""
            echo "🔍 Running health checks..."
            # Check backend health with retry
            BACKEND_OK=false
            for i in {1..5}; do
              if curl -f -s --max-time 5 http://localhost:8000/api/users > /dev/null 2>&1; then
                echo "✅ Backend health check passed (attempt $i)"
                BACKEND_OK=true
                break
              fi
              echo "⏳ Backend not ready yet, retrying... ($i/5)"
              sleep 3
            done
            
            if [ "$BACKEND_OK" = false ]; then
              echo "❌ Backend health check failed - rolling back"
              docker-compose restart
              exit 1
            fi
            
            # Check frontend health (optional - frontend deployed manually)
            FRONTEND_OK=false
            for i in {1..3}; do
              if curl -f -s --max-time 5 http://localhost:3001 > /dev/null 2>&1; then
                echo "✅ Frontend health check passed (attempt $i)"
                FRONTEND_OK=true
                break
              fi
              echo "⏳ Frontend not ready yet, retrying... ($i/3)"
              sleep 2
            done
            
            if [ "$FRONTEND_OK" = false ]; then
              echo "⚠️ Frontend health check failed - but continuing (frontend deployed manually)"
            fi
            
            echo ""
            echo "📊 Running database migrations..."
            docker-compose exec -T backend-app php artisan migrate --force || echo "⚠️ Migration may have failed or already run"
            
            echo ""
            echo "✅ Deployment successful - all services running"
            echo "⏰ End time: $(date)"